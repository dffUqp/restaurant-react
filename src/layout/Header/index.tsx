import Container from '../../components/Container';
import Navbar from '../../components/NavBar';

const Header = () => {
  return (
    <header>
      <Container>
        <Navbar />
      </Container>
    </header>
  );
};

export default Header;
